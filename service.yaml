apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: flachwitz-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30171
  type: NodePort
